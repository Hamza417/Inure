<resources>
    <!-- Base application theme. -->
    <style name="Inure" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorAppAccent">@color/inure</item>
        <item name="colorAppAccentLight">@color/inure_light</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/statusBarColor</item>
        <item name="android:navigationBarColor">@color/statusBarColor</item>
        <item name="android:colorEdgeEffect">?attr/colorAppAccent</item>
        <item name="android:windowTranslucentNavigation">false</item>
        <item name="android:windowTranslucentStatus">true</item>
    </style>

    <style name="Inure.Transparent" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorAppAccent">@color/inure</item>
        <item name="colorAppAccentLight">@color/inure_light</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <style name="InfoTextSmall" parent="Widget.AppCompat.TextView">
        <item name="android:textSize">@dimen/info_text_small</item>
        <item name="android:textColor">@color/textTertiary</item>
    </style>

    <style name="InfoTextLarge" parent="Widget.AppCompat.TextView">
        <item name="android:textSize">@dimen/info_text_large</item>
        <item name="android:textColor">@color/textHeading</item>
        <item name="android:layout_marginTop">@dimen/info_text_large_margin_top</item>
        <item name="android:layout_marginBottom">@dimen/info_text_large_margin_bottom</item>
    </style>

</resources>
